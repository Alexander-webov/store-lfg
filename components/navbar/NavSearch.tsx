import { Input } from "../ui/input";

function NavSearch() {
  return (
    <Input
      className="max-w-xs"
      type="search"
      name="search"
      placeholder="search"
    ></Input>
  );
}

export default NavSearch;
